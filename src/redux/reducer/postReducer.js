// test
console.log('REDUCER - POST: tich hop thanh cong !');

const initStatePost = [];

export const postReducer = (state = initStatePost, action) => {
    switch (action.type) {
        // case value:

        //     return state;

        default:
            return state;
    }
}